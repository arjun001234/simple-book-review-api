### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Author {
  books: [Book]!
  id: ID!
  name: String!
}

type Book {
  authors: [Author!]!
  description: String!
  id: ID!
  isbn: String!
  price: Float
  reviews: [Review!]!
  title: String!
}

input BookInputType {
  authorName: String!
  description: String!
  isbn: String!
  price: Float
  title: String!
}

"""
A date string, such as 2007-12-03, compliant with the `full-date` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar Date

type Mutation {
  createBook(data: BookInputType!): Book!
  createReview(data: ReviewInputType!): Review!
}

type Query {
  author(id: ID!): Author!
  authors: [Author!]!
  book(id: ID!): Book!
  bookReviews(id: ID!): [Review!]!
  books: [Book!]!
  searchAuthors(name: String!): [Author!]!
  searchBooks(title: String!): [Book!]!
}

type Review {
  book: Book!
  content: String!
  createdAt: String!
  id: ID!
  reviewer: Reviewer!
}

input ReviewInputType {
  bookId: ID!
  content: String!
  email: String!
  name: String!
}

type Reviewer {
  email: String!
  id: ID!
  name: String!
  reviews: [Review!]!
}
